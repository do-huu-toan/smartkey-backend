#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../typeorm-model-generator/bin/typeorm-model-generator" "$@"
  ret=$?
else 
  node  "$basedir/../typeorm-model-generator/bin/typeorm-model-generator" "$@"
  ret=$?
fi
exit $ret
